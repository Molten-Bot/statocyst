name: deploy-prod

on:
  workflow_dispatch:

concurrency:
  group: deploy-prod
  cancel-in-progress: false

permissions:
  contents: read

env:
  IMAGE_REPO: docker.io/${{ vars.DOCKERHUB_USERNAME || secrets.DOCKERHUB_USERNAME }}/statocyst

jobs:
  guard-main:
    runs-on: ubuntu-latest
    steps:
      - name: Enforce main branch only
        run: |
          if [ "${GITHUB_REF}" != "refs/heads/main" ]; then
            echo "ERROR: production deploys must run from main. Current ref: ${GITHUB_REF}" >&2
            exit 1
          fi

  test:
    runs-on: ubuntu-latest
    needs:
      - guard-main
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22.x'

      - name: Run tests
        run: go test ./...

  build-and-push:
    runs-on: ubuntu-latest
    needs:
      - test
    outputs:
      image_ref: ${{ steps.image.outputs.image_ref }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME || secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE_REPO }}:prod-${{ github.sha }}
            ${{ env.IMAGE_REPO }}:latest

      - name: Export image reference
        id: image
        run: echo "image_ref=${IMAGE_REPO}:prod-${GITHUB_SHA}" >> "$GITHUB_OUTPUT"

  deploy:
    runs-on: ubuntu-latest
    needs:
      - build-and-push
    environment: prod
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Trigger Prod deploy hook
        env:
          DEPLOY_HOOK_BEARER_TOKEN: ${{ secrets.DEPLOY_HOOK_BEARER_TOKEN }}
        run: |
          scripts/release/trigger_deploy_hook.sh \
            "${{ secrets.DEPLOY_HOOK_URL }}" \
            "prod" \
            "${{ needs.build-and-push.outputs.image_ref }}" \
            "${{ github.sha }}"

      - name: Optional Prod health check
        if: ${{ vars.HEALTHCHECK_URL != '' }}
        env:
          HEALTHCHECK_URL: ${{ vars.HEALTHCHECK_URL }}
        run: |
          for i in $(seq 1 30); do
            code="$(curl -s -o /dev/null -w "%{http_code}" "$HEALTHCHECK_URL" || true)"
            if [ "$code" = "200" ]; then
              echo "health check passed"
              exit 0
            fi
            sleep 2
          done
          echo "ERROR: production health check failed for $HEALTHCHECK_URL" >&2
          exit 1
