interface:
  display_name: "Hub Update Agent Profile"
  short_description: "Update agent metadata on Hub API"
  default_prompt: "Use $hub-update-agent-profile with the stored api_base or hub session file to replace authenticated agent metadata via PATCH /v1/agents/me/metadata."
