interface:
  display_name: "OpenClaw Bind Agent"
  short_description: "Register agent and create or join a peer bond"
  default_prompt: "Use $openclaw-bind-agent to register agent_id=crab and bond with from_agent_id=shrimp."
